subprojects {
    buildscript {
        repositories {
            flatDir {
                dirs rootProject.file( '../build/libs' )
            }
            jcenter()
        }

        dependencies {
            classpath "com.athaydes.gradle.pony:pony-gradle-plugin:0.2.0"
        }
    }
}
